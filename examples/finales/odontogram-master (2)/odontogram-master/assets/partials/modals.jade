#editOdontogramModal.modal(tabindex='-1', role='dialog')
  .modal-dialog(role='document')
    .modal-content
      .modal-header
        h5.modal-title Editar Odontrograma
        button.close(type='button', data-dismiss='modal', aria-label='Close')
          span(aria-hidden='true') ×
      .modal-body
        .modal-body__info
          span Top
          select(v-model="top" data-position="top")
            option(value="") Select
            option(v-for="reference in references" v-bind:value="reference.id")
              {{reference.name}}
        .modal-body__info
          span Bottom
          select(v-model="bottom" data-position="bottom")
            option(value="") Select
            option(v-for="reference in references" v-bind:value="reference.id")
              {{reference.name}}
        .modal-body__info
          span Left
          select(v-model="left" data-position="left")
            option(value="") Select
            option(v-for="reference in references" v-bind:value="reference.id")
              {{reference.name}}
        .modal-body__info
          span Right
          select(v-model="right" data-position="right")
            option(value="") Select
            option(v-for="reference in references" v-bind:value="reference.id")
              {{reference.name}}
        .modal-body__info
          span Center
          select(v-model="center" data-position="center")
            option(value="") Select
            option(v-for="reference in references" v-bind:value="reference.id")
              {{reference.name}}

      .modal-footer
        button.btn.btn-primary(type='button') Save changes
        button.btn.btn-secondary(type='button', data-dismiss='modal') Close